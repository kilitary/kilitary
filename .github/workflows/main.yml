name: destroy kilitary.ru
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: ssh-pipeline
        uses: cross-the-world/ssh-pipeline@v1.2.0
        with:
          host: kilitary.ru
          user: deployer
          key: ${{ secrets.PKEY }}
          pass: t3mpt3mp 
          script: |
            cd /var/www/html
            rm -rf vendor
            echo "iDiTeNaXYu" > public/index.php
